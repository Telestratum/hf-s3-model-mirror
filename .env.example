# Hugging Face token with access to the gated model repos
HF_TOKEN=

# AWS region used by boto3 (or set via your AWS config/profile)
AWS_REGION=us-east-1

# Optional: if using static creds (not recommended for production)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=
